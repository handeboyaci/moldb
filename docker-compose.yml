services:
  app:
    build:
      context: .
      args:
        BUILD_ENV: dev
    expose:
      - "8000"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    volumes:
      - .:/app
      - ./uploads:/uploads
    networks:
      - default
      - nginx_npm_network

  db:
    image: mcs07/postgres-rdkit
    environment:
      - PYTHONPATH=/app
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=chemstructdb

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d chemstructdb"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7.2

  worker:
    build:
      context: .
      args:
        BUILD_ENV: dev
    entrypoint: ["rq", "worker", "-u", "redis://redis:6379"]
    depends_on:
      - redis
      - db
    volumes:
      - .:/app
      - ./uploads:/uploads

networks:
  nginx_npm_network:
    external: true

